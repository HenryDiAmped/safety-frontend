export const environment = {
  production: true,
  emailjs: {
    // Reemplaza estos valores con tus credenciales reales para producción.
    serviceId: 'REPLACE_SERVICE_ID',
    templateId: 'REPLACE_TEMPLATE_ID',
    publicKey: 'REPLACE_PUBLIC_KEY',
  },
};
